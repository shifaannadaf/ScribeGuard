// This file used to hold UI mock records. ScribeGuard is a production system
// and the dashboard renders only real data fetched from the backend. The file
// is intentionally left empty so any stale import fails loudly.
export {}
