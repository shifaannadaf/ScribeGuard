fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
openai>=1.54.0
httpx>=0.27.0
tenacity>=9.0.0
structlog>=24.4.0

# Local provider (SERVICE_PROVIDER=local). Free on-device Whisper port.
# Ollama itself is a separate install — see README.
faster-whisper>=1.0.3

# Transcript import — text extraction from PDF / DOCX uploads.
pypdf>=4.0.0
python-docx>=1.1.0
fpdf2>=2.8.0
